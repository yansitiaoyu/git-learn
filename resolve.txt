克隆远程仓库时，使用加速器watt toolkit会出现端口22的报错
git中本地仓库与远程仓库链接时，分支关联时git push -u origin main命令需要换为git push --set-upstream origin main才不会报错
遇到warning: LF will be replaced by CRLF报错时，是因为跨系统回车（CR）和换行（LF）的不同导致的，输入命令git config --global core.autocrlf true可解决此问题
